[project]
name = "music-analysis"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "librosa>=0.11.0",
    "mido>=1.3.3",
    "music21>=9.9.1",
    "setuptools>=82.0.1",
]

[tool.uv.extra-build-dependencies]
vamp = ["numpy"]
